import styled from 'styled-components';

import * as T from 'styles/typography';

export const WarmingWrapper = styled.div`
  text-align: center;
`;

export const WarmingText = styled.p`
  ${T.BODY_TEXT_LARGE}
`;
